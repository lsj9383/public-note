package com.lsj.util;

import java.util.Iterator;

public interface LList extends LCollection{
	/* 从LCollection中继承的方法 */
	int size();
	Iterator<T> itertor();
	boolean contains(T o);
	boolean add(T nv);
	boolean remove(T o);
	
	
	boolean add(int idx);
}
