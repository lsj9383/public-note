一、
二、创建和使用数据库
	1.数据类型
		所有的流行数据库都可以存储同样的基本数据类型，对于特殊的类型比如大文档及二进制文档则有较大的差异。
		1).字符型数据
			可以定长或变长。
			定长：char(length)，不达length时将会用空格填充。length应该不超过255.
			变长：varchar(length)，length 应该不超过65535。
			如果需要更长的字符串，则采用longtext或mediumtext。
		2).数值型数据
			